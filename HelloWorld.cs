using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //nu ya hz cho eto pon
        print("hello world");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
